name: inspector_core
description: Core utilities for Flutter apps - BaseBloc, CommonCubit, Exceptions, Navigation
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  go_router: ^14.6.0

# Note: Network functionality is in the separate `inspector_network` package
